# Plan

1. Add failure-first tests for strict result receipts and arm identity binding.
2. Implement bounded canonical parsing, derived result identity and plan admission.
3. Export the API and document the offline-only measurement boundary.
