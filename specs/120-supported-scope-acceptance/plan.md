# Plan

Use immutable113 mathematical cases/oracle/holdouts and runtime accounting, with a new task_scope
module that replaces only the common goal suffix. Use unchanged117 worker/response wrapper and
fork the launcher into120. Keep all117 model, budget, sampling, population and scheduling fields;
explicitly declare product, task-goal and primary-definition changes. Pin113/115/117 history.

The new source_analysis adapter uses the old read-only output/delivery/holdout analyzer, then
verifies the compiled source contract and new portable source evidence. Require the registered
hard Python file-count minimum2, supported remaining capabilities and the source-aware protocol.
Do not accept merely two files under a legacy delivery or a dropped compiler requirement.
Output holdouts audit output schema/harness only; they do not audit execution behavior.

Data model: manifest has fixed cases, limits, optima, holdouts, source/measurement/history pins,
provider-safe metadata and declared before/after changes. Existing once-only start/finish markers,
request journals and worker receipts remain. Postrun results add explicit source verification;
portable evidence retains full hashes. No product changes, dependencies or migration.

600 seconds bounds each HTTP call and normal Agent invocation; tool calls share the latter's
remaining time. The external process supervisor bounds the entire task at3600 seconds. The
160000 observed-token threshold stops later requests and rejects the triggering response;
it is not a server token cap. Timeout consumption and monetary cost may be unknown.

Validate routing, once-only slots, immutable conditions, supported source contract, independent
outputs, evidence downgrade/tamper rejection and null failure quality offline. Run source-aware
pipeline fixtures and the112 quickstart. Independently review registration before freezing/pushing,
then launch each slot once and audit retained evidence. Product119 already passed5912 tests/1skip;
measurement/docs changes need focused regression, not another unchanged full-product suite.

Alternatives rejected: replay unsupported behavioral tasks as if file count proves them, silently
exclude unsupported requirements, reopen old slots, enlarge budgets again, or retry until success.
No constitution exception. Local candidate execution is trusted-owner execution, not an OS sandbox;
registered holdout sources are excluded from supplied model context but locally accessible.
