# Solver Profile Contract

Named metadata is runtime-neutral: `name`, `description`, `required_capabilities`, and default
budget. Profiles never spawn processes or inspect global Hermes/OpenCode state.
