# Evaluator Profile Contract

An evaluator profile names a factory returning the existing structured `Evaluator` protocol. Built-in
profiles include `general` (non-empty), `data`, `research`, and `coding`; callers may inject a custom
registry or evaluator.
