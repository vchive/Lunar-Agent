# Plan

Reuse the small128 task mathematics and holdout executor in a new campaign-local case module.
Register a plain-language goal with explicit output/source scope; validate the generated contract
without supplying a manual contract to native solve. Copy128 registration and process supervision
patterns, adapting fixed conditions to the full solve. Reuse passive113 request accounting and128
transport capture with correct contract/compiler/auditor/candidate stage labels. Provider details
are loaded only after committed/pushed registration checks; clean child environment fixes native
retries and timeout. Product remains byte-identical throughout the measurement.

Worker stages exact input bytes and invokes unchanged native CLI with registered population and
runtime options. It retains native JSON privately. Once solve returns, read-only Store projections
validate any available automatic preparation and contract; only then execute the eight registered
holdouts once, within the supervisor's original deadline. Incomplete/failed stages remain visible.

Analyzer follows the verified parent/child/artifact/delivery/source evidence checks in120, but
does not reuse its old task-specific analyzer or execute holdouts. It revalidates worker frozen
identity, durable preparation diagnostics, output math, request usage and retained holdout rows.
Publish bounded public results and a complete file size/hash inventory once. No migrations,
dependencies or public runtime API changes; no constitution exception.
Read SQLite through a temporary DB/WAL copy so rebuilding SQLite shared memory after a killed
writer cannot modify retained state.db-shm. Do not use immutable mode, which can omit WAL rows.

Offline tests cover successful full CLI delivery with local fixtures, source/contract/output drift,
failure before/after preparation, ledger/capture accounting, once-only holdouts/summary, registration
drift and push requirements in disposable Git, and process cleanup. Run relevant current regression
and112 terminal recovery example; no full-product rerun without product edits or a new concern.
Independent review precedes registration. Freeze implementation/tests, prepare/verify, commit/push,
run once, summarize once, independently audit retained evidence, document and push the outcome.
