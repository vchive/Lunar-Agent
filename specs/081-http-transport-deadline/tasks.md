# Tasks: Absolute HTTP Transport Deadline

- [x] T081-01 Reproduce socket timeout overrun locally and assess cancellable transport alternatives.
- [x] T081-02 Approve process/lifeline/IPC/compatibility specification before implementation.
- [x] T081-03 Add failure-first real child/loopback deadline and lifecycle tests.
- [x] T081-04 Implement bounded transport supervisor/worker and runtime integration.
- [x] T081-05 Verify no late success, credential exposure, orphan work, retries or failed-subject scoring.
- [x] T081-06 Independently review and run targeted/full/lint/packaging/SDD checks.
- [x] T081-07 Verify old seals, update HANDOFF/README and commit/push closure.

No real model provider, WebAgent, company platform, private harness or prior candidate is executed.
078 remains valid0/2 with unknown full failure usage/cost; this fix cannot determine its old cause.

## Failure-first evidence

Implementation owner ran the first16 HTTP/lifecycle scenarios on the old implementation:
15 failed /1 passed in33.36 seconds; the old slow HTTP error-body path dominated the duration.
Root independently added5 public HTTPS tests. After correcting test endpoint normalization and
isolating urllib's cached opener between trust fixtures, they produced2 failed /3 passed in1.69
seconds: finite calls still inherited SSLKEYLOGFILE, and the HTTPS trickle was accepted after the
requested0.3 seconds. File/directory CA trust, untrusted-certificate rejection and None behavior
were already passing. The certificate/key are disposable local fixtures generated byopenssl, not
user/provider credentials; no external TLS endpoint was contacted.

Independent review then found that an explicit generic SSL context dropped urllib's default
HTTP/1.1 ALPN configuration. Root added two loopback ALPN cases:1 failed /1 passed in0.40 seconds
(finite timeout negotiated no protocol; None negotiated http/1.1). Using the default HTTPSHandler
restored standard-library TLS behavior while preserving the projected CA settings; the updated
HTTP deadline and HTTPS modules passed54 cases in4.13 seconds.

Root also identified the select FD ceiling in the new pre-dispatch lifeline check. The
implementation owner reproduced it using a real descriptor at least1024 (1 failed in0.16 seconds),
then changed the check to DefaultSelector. All55 HTTP/HTTPS cases passed in4.08 seconds.

Final review reproduced late failure acceptance, deeply nested malformed IPC and bounded exception
chain differences:5 failed /9 passed in0.65 seconds, plus1 failed /3 passed for divergent reason/cause
branches. Deadline rechecks now include failure terminals; malformed nesting becomes a fixed safe
failure. Two additional fixed internal cause values preserve typed reason and legacy code
independently, without modifying subject_diagnostics or sending exception objects over IPC.

## Final verification (2026-09-11)

- New coverage:66 HTTP deadline/lifecycle/protocol cases and7 HTTPS cases.
- Implementation focused/native normal/deep/staged/runtime/budget checks:464 passed in28.48 seconds.
- Independent final HTTP/HTTPS review:73 passed in4.72 seconds; earlier base review245 passed.
- Root full main regression, run once after final implementation: **1687 passed in50.45 seconds**.
- Full src/tests Ruff, Specify081 prerequisites and git diff checks passed. Independent review
  found no remaining blocker and confirmed constitution, diagnostic and scoring boundaries.
- Offline wheel build and a separate Python3.13.12 virtual environment succeeded with no runtime
  dependencies. Public complete loaded from installed site-packages and launched the packaged
  isolated helper: one fast success and one0.3-second trickle rejected after0.3041 seconds with
  read_response_body/transport_timeout. Exactly two loopback requests; no provider credentials in
  the smoke environment or external provider calls. The existing setuptools license-table
  deprecation warning did not prevent the build; it is outside this transport change.
- All Git-sealed078/076/074 files match a80f9b8/9ad2e1c/68b5e58 byte for byte:205/105/68 files.
  No old live-source audit was rerun against changed product source.

Validation is macOS evidence. Finite calls add process and cleanup overhead, retain the default
stdlib proxy/TLS behavior with the documented projection, and do not inherit custom in-memory
openers. None remains direct urllib. Parent parsing/cleanup is not a hard realtime complete()
deadline, and stopping the client does not guarantee remote generation/billing termination.

No real evaluation was launched. The next measurement needs a new frozen registration for the
current product with GLM-5.2 and the original effect/harness authority. Prior078 remains valid0/2;
scores and complete failed usage/cost stay unknown. Offline deadline tests do not establish why
078 failed or demonstrate improved Build delivery.
