Focused validation is recorded with the implementation commit. The required set is
`tests/test_producer_bundle_receipts.py`, `tests/test_producer_bundle_staging.py`, and
`tests/test_producer_bundle_recovery.py`, followed by Ruff and compileall. No provider or external
producer is started.
