# Plan

1. Freeze the task envelope schema and offline mapping fixtures.
2. Add strict parser, canonical/comparison digests and read-only input/pin admission with failing tests first.
3. Add static `benchmark-task validate` CLI dispatch and bounded JSON errors.
4. Update README, architecture, roadmap and HANDOFF; run focused/full/static validation and commit locally.
