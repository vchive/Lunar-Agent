# Plan

Copy the six125 measurement modules into an independent128 directory. Keep case and supervision
byte-identical and observer behavior identical except its private import name. Campaign pins new
identity/root/product, previous125 reference and history, and explicitly declares local diagnostic
capture. All existing fixed mathematical/runtime/provider/budget/sampling conditions match125.
The first request hash reflects126's changed prompt. Product remains fixed at127 throughout.

Worker projects only exact typed native preparation diagnostics into optional local_failure in its
existing worker-finished record; failure to project is null and cannot replace failure/cleanup.
Analyzer revalidates fixed enum/index keys and consistency with failed preparation/no freeze and
the completed compiler/auditor request ledger, publishing safe detail only. Keep no exception prose.
No new runtime/data model/dependency or migration; no constitution exception.

Before registration use fresh offline compiler/auditor fixtures: valid target-based evaluator with
14local probe/holdout executions; scalar compiler inputs rejected before any harness or audit;
correct compiler and invalid auditor probe/response retained with exact stage/reason/positions.
Test once-only storage, malformed local detail, role mismatch, unknown usage, response observation,
cleanup and summary without execution. Registration tests use real disposable Git repositories and
synthetic product commits, not another frozen-version dependency on current src.

Review independently; freeze measurement/test files, prepare/verify locally, commit and push all
pins, then run exactly once. Observe safely at coarse intervals, never reopen or replace the slot.
Summarize once, independently audit retained hashes/accounting/outcome, update handoff/status and
push results. Product127 full regression already passed; run focused measurement/product checks
and112 quickstart for these measurement-only changes. No full-product rerun without new concern.
