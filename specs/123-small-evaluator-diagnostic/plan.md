# Plan

Keep product files unchanged. A campaign-local case module constructs the typed contract, staged
descriptor and native value-free input profile. It declares8 holdouts and an independent arithmetic
oracle; execute them under native _snapshot_probe only after loading/revalidating the frozen bundle.
The requested diagnostic runtime is AgentLoopRuntime around OpenAICompatibleRuntime, preserving
the isolated system+user pair used by automatic preparation. Do not use direct .run materialization.

Reuse the frozen113 RuntimeGuard admission/accounting through an explicit importlib file loader.
A new optional observer retains bounded assistant text and safe exchange metadata after the native
outcome. Wrap lunar_evolution.runtime.exchange only within the single worker and return/rethrow the same
objects; measure response and failure without changing bytes, parameters, retries or deadlines.
Record role from the known isolated call sequence, not prefix heuristics (snapshot prefix differs).
Restore patches on every exit. Raw captures are local private diagnostics, not scored evidence.

Supervisor launches one new-session worker, enforces1320s over HTTP plus local code, reaps and
checks owned observed processes. Worker markers claim before provider configuration; durable
stage/request/freeze/holdout receipts support outcome reconstruction after interruption. Input/profile
and frozen evaluator fingerprints are rechecked. Postrun analysis is read-only: no model, solver,
holdout rerun or resume. New immutable public inventory/results files record the measured outcome.

Validate fake/local success, compiler rejection/timeout, conditional auditor, wrong frozen evaluator,
unknown usage, passive observation, once-only markers, registered conditions/pins, deadline cleanup
and report projection before manifest preparation. Independent review precedes commit/push/launch.
Record results and recheck historical bytes after execution; push the completed report. Product122
already passed6200 tests/1skip; focused measurement/runtime regressions suffice without product edits.

No new dependencies, migrations or constitution exception. Do not increase600s again, add streaming,
probe provider availability, replay120 or treat absent data as zero. Diagnostic boundaries are
versioned separately; no old registration or reported denominator is updated.
