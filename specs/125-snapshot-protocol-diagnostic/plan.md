# Plan

Copy the six 123 campaign-local modules into a new125 directory. Keep public case math/input/
holdouts byte-identical, along with worker/analysis/supervision behavior; change only campaign/module
identity and registered product/history/reference metadata. Do not execute old campaign commands or
the private 123 model-generated source. Require the new fixed mathematical conditions to match the
immutable prior manifest, while the compiler request hash reflects the new124 prompt.

Reuse the independently tested113 accounting and optional observer without changing request bytes,
deadlines or outcome handling. Persist exclusive markers, contract/profile, safe responses/transport,
freeze and per-holdout receipts in the new root. Safe analysis publishes inventory/results once.
No product/data model/dependency/migration or constitution change.

Before registration, exercise the125 modules with fresh synthetic target-based source and the
wrong-path variant, native isolated fake transport, actual local probes/holdouts, once-only claims,
supervisor cleanup, unknown usage and safe read-only summary. Verify observer/case/supervision and
unchanged function bodies against123; focused existing tests cover identical components. New
registration fixture tests use a temporary Git repository and fake provider metadata, so they do
not introduce another test dependency on the mutable working product or edit old pinned tests.

After independent review, freeze new measurement/test bytes, prepare/verify the manifest offline,
commit/push all registration pins, then run the one real attempt. Observe progress without resuming
or replacing it. Summarize and verify evidence/history after completion, document limits and actual
outcome, and push results. Product124's full two-stage regression already passed; focused
measurement/runtime regression and the112 quickstart are appropriate for measurement-only edits.
