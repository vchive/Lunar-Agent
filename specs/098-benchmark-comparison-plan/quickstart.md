# Offline comparison plan

A future static validator will accept a plan only after every arm is checked against the same task,
contract, model profile, exact evaluator and physical budget. This feature freezes conditions; it
does not execute a benchmark or compare effectiveness.
