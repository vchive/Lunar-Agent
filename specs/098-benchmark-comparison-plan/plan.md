# Plan

1. Add failing tests and freeze the multi-arm comparison plan schema.
2. Implement strict parser, derived comparison identity and read-only per-arm admission.
3. Review compatibility with 097 and existing BenchmarkRunner without adding a framework runner.
4. Update docs/HANDOFF and run focused/static validation before local commit.
