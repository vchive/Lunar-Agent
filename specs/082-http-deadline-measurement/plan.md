# Plan: HTTP Deadline Fixed Measurement

## Design and ownership

Use the reviewed078 wrapper structure, directly binding the pinned074 engine rather than adding
another historical loader layer. Change campaign/source/path identity, history anchors and safe
terminal reporting only. Freeze e36b103's38 product modules, the complete helper closure and all
execution/test/input bytes. The sealed078 chain is the immediate historical source; its profile,
limits and cases remain unchanged. Main has no active campaign; product files remain untouched.

Root owns SDD, actual registration, final preaudit/dry-run, commit/push, unique launch and observation.
Implementation agent owns new082 measurement/postrun scripts and new082 tests. Independent review
is read-only. Neither agent loads credentials or starts a real attempt. Once launched, all frozen
files remain immutable; fixes, if necessary, use an isolated worktree until results are sealed.

## Contracts and decisions

Reuse the native request/receipt, workflow ledger, immutable trial record and exact harness schemas.
Do not create alternative result acceptance. The public subject receives only the existing public
projection. Copy only case/profile metadata and input bindings from078, never prior candidate files.
Rebind check_source's actual function globals and verify new http_transport.py cannot be omitted.

The new readiness projection also binds the subject console launcher's actual shebang interpreter,
resolved binary digest, Python/SSL versions, actual runtime/helper imports and isolated helper argv.
Use a clean local interpreter inspection only, with no HTTP request or credential access; verify
the frozen projection before dispatch. Do not confuse the root interpreter with the actual subject.

Keep v1–v4 terminal diagnostic compatibility through the current frozen validator. Any report
extension must bind diagnostics to the actual subject request and native failure record and freeze
its evidence hash; missing/malformed diagnostics remain absent or safely degraded according to the
existing validator. Never deserialize arbitrary exceptions or inspect raw transcript content for
new model-failure projections. Terminal HTTP timing is advisory, not an extra receipt or score.

The inherited dry-run audit hook guards its pytest process and is not inherited across exec.
Select deterministic model, pure IPC and schema fixtures for this guarded run. Run actual081
loopback HTTP/TLS checks separately and label that boundary accurately; do not claim the inherited
hook prevents child network access. Keep diagnostic reads inside the existing Evidence stability
check and retain its refusal of symlinks and source drift.

The existing exclusive launcher records ownership before loading CC Switch credentials, enforces
the frozen model/endpoint identities and starts one fixed wave. No connectivity probe is needed.
Retain original process-group containment: finite HTTP workers inherit the subject PGID and own a
lifeline. Final process checks must account for the new helper as well as known subject/harness PIDs.

## Sequence and runnable offline quickstart

1. Implement failure-first082 source/helper/path/history/diagnostic registration and native lifecycle
   tests. Independently review; run focused/full checks, Ruff, Specify and diff checks.
2. Root materializes once, verifies real local bytes/dependencies, runs the independent preaudit and
   guarded dry-run, mirrors reports and commits/pushes the registration before any start marker.
3. Launch once. Preserve read-only progress and partial audits; do not alter running conditions.
4. Require both terminal slots for final acceptance, audit native results/hash/process evidence,
   retain null fields, and commit/push the seal before integrating any later product change.

```sh
.venv/bin/python -m pytest -o addopts='' -q tests/test_measurement082_audit.py tests/test_measurement082_runner.py specs/082-http-deadline-measurement/postrun/test_audit.py
.venv/bin/python -m pytest -o addopts='' -q
.venv/bin/ruff check specs/082-http-deadline-measurement tests/test_measurement082_audit.py tests/test_measurement082_runner.py
bash .specify/scripts/bash/check-prerequisites.sh --json --require-tasks --include-tasks
git diff --check
```

These are offline fixture commands. Actual prepare/launch is owned by root and follows reviewed
script contracts; importing helpers must never dispatch or access provider credentials.

## Constitution and alternatives

No exception or migration. Reusing the native runner, receipt and evaluator authority preserves IV;
exclusive bounded dispatch and protected credentials preserve V; failure-first source/lifecycle/
diagnostic tests preserve VI. A new HTTP probe would add an unregistered model call without proving
task delivery. Reopening078 would invalidate its sealed denominator. A new registration preserves
both histories while directly observing current Build delivery. Aggregate quality means, causal
deltas, failed-request usage recovery and general stability claims remain out of scope.
