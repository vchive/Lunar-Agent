# Feature 076: Public Plan Handoff Measurement

## Problem and acceptance

Feature074's postal Master passed JSON parsing but its public objective vocabulary was rejected
by a semantic keyword filter; sheet Master timed out at1200 seconds. Feature075 removed that
filter and retained structural, credential, path and native receipt authority checks. Its1316
offline passing tests establish implementation behavior, not real solution validity. Measure the
corrected source using two fresh fixed attempts and separate accepted planning, Build and scoring.

1. Freeze product96d5a600f03e5401753efc6f579d2ff14fe839be, GLM-5.2 and the existing authorized
   CC Switch endpoints/profile/public case projections/private exact harness. No WebAgent run,
   company-platform query, stronger model, provider probe or historical candidate/plan replay.
2. Exactly two new S attempts: slot1 sheet_metal_nesting and slot2 china_post_pickup_optimization.
   Both budget_arm=master_1200, one wave[[1,2]], concurrency2. Campaign is
   .lunar/real-eval-glm-5.2-public-plan-20260910. No retry, replacement or old-attempt reopening.
3. Keep Master1200/build2400/reserve120 seconds and checkpoint_after_rounds32; at most one
   same-process cooperative continuation. Shared5400 seconds/200 tools/8000000 cumulative tokens,
   unknown cost ceiling null. Outer subject5430/harness3630/slot9300 seconds. No prompt, role,
   model or budget redesign in this measurement.1200 seconds is not presumed sufficient.
4. Before launch, independently review executable wrappers, run isolated offline fixtures,
   materialize registration, independently preaudit/dry-run, mirror reports, commit and push.
   Freeze all source, new executable/test/input bytes and complete pinned069/072/074 helper closure.
   Check actual public/private/harness content and installed versions, not only saved digests.
5. Original EffectTrialRunner owns public-input and subject receipt validation and the sole exact
   harness dispatch/score authority. Observe valid Master record, Build evidence and native validity
   separately. Never infer a valid solution from a plan, candidate, local score or checkpoint.
6. Keep unknown scores/complete failed usage/cost and unavailable precise Master duration null.
   Preserve above-one native scores. Fixed denominator2, historical069/072/074 context excluded,
   no pooled quality mean, no concurrent-control or causal/stability claim from one attempt/case.
7. Finish both attempts and independently audit final native chain, registered source/dependencies
   and observed process termination before sealing results. Make any further fixes in an isolated
   worktree until this campaign is sealed. Do not update old manifests to match changed source.

## Scope and limitations

Only measurement registration/orchestration changes. Reuse pinned validated helpers through small,
explicit private module bindings; do not transform old manifests or duplicate a new evaluator.
Network/provider variability, cache/resource contention, coarse stage timing and bounded process
visibility remain limitations. Public projection is not an OS sandbox. This new run measures the
current configuration descriptively; it does not isolate a causal effect of075 or fix timeouts.
